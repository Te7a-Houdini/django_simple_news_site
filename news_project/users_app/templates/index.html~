<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
<style type="text/css">
        body{
            font-family: Arial;
            font-size: 16px;
        }
        .post{
            padding: 50px;
            font-size: 14px;
            border: 1px solid #ddd;
            border-radius: 20px;
            width: 765px ;
            height: 100px;


            margin-right: auto ;
        }
        .cat{
             padding: 20px;
            font-size: 16px;
            border: 1px solid #ddd;
            border-radius: 20px;
            text-align: center;
            height: 5px;
            float:right;
            display:inline;
            width: 200px;
            margin:5px;

        }
        .tb{
            float:left;
            display:inline;
            padding-left:40px;

        }

    </style>
</head>
<body>
<!-- looping For Each post -->
{% for post in posts %}

    <!-- showing some of the content -- >
    {{ post.content  | safe| truncatewords:30 }}

    <!-- getting every tag for the post-->
    {% for tag in post.tag.all %}
     <a href="#ok">   {{ tag }} </a>
        {% endfor %}
{% endfor %}

<!-- pagination-->
<table class="tb">
{% for pag in walk %}
{% for tg in users_app_post_tag %}
<tr>
<td><div class="post">
  {{ pag.post_category| safe}}<br>
  {{ pag.title| safe}}<br>
  {{ pag.content| safe}} : at : 
  {{ pag.created| safe}}<br>
  <img src=pag.image | safe><br>
   {{ tg.tag_id | safe}}
</div></td></tr>
{% endfor %}
{% endfor %}
<tr><td><div align="center">
 <span>
    {% if walk.has_previous %}<a href="?page={{  walk.previous_page_number }}">previous</a>{% endif %}
    <span>Page {{ walk.number }} of {{walk.paginator.num_pages }}.</span>
     {% if walk.has_next %}<a href="?page={{ walk.next_page_number }}">next</a>{% endif %}
 </span>
</div>
{{ page.render }}</td></tr>
<br>

</table>
</body>
</html><div class="cat">cat1</div><br><br>
<div class="cat">cat2</div><br><br>
<div class="cat">cat3</div>
